<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:orientation="vertical"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.daniel.haughton93.dunnesstoresoffers.MainActivity">
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="20dp"
        android:visibility="gone"
        android:text="You need to give this app permission to read from your sd to work!"
        android:textStyle="bold"
        android:id="@+id/textViewPermissions"
        android:textSize="20sp"/>
    <Button
        android:layout_height="100dp"
        android:layout_width="match_parent"
        android:text="REQUEST PERMISSIONS AGAIN"
        android:textSize="20sp"
        android:visibility="gone"
        android:id="@+id/buttonRequestPermissions"
        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="20dp"
        android:text="Saved Offer Leaflets"
        android:textStyle="bold"
        android:textSize="30sp"/>

    <ListView
        android:id="@+id/listViewPdfs"
        android:layout_width="match_parent"
        android:layout_height="100dp" />

    <Button
        android:layout_height="100dp"
        android:layout_width="match_parent"
        android:text="Check for latest offers"
        android:textSize="20sp"
        android:id="@+id/buttonCheckLatestOffers"
        />
</LinearLayout>
